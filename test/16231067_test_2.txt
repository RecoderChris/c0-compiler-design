const int con1=1,con2=2;
int array[100];

int add(int a, int b, int c, int con1, int con2){
    return (a + b + c + con1 + con2);
}

int factorial(int n){
    if (n == 0) 
        return (1);
    else
        return (n* factorial(n - 1));
}

void exp{ 
    array[1] = con2 / 1 + 1 + add(1, 2, 3, 4, 5);
    printf( - array[1] + +005 + 'a' + factorial(3) + -1 * 3-con1);
}

void main(){
	exp;
	return;
}